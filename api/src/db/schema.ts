// Drizzle ORM schema definitions for D1 (SQLite)
// Add table definitions here as the application grows.
// Example:
//   import { sqliteTable, text, integer } from "drizzle-orm/sqlite-core";
//   export const users = sqliteTable("users", { ... });
